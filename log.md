# InvestCool 调试与部署复盘日志 (2026-03-12)

## 问题现象描述
1. **修改不生效**: 尽管修改了源代码，但前端页面依然显示旧版英文内容。
2. **502 Bad Gateway**: 重启服务后，通过域名访问返回 502 错误。
3. **API 数据缺失**: 纳斯达克指数最初为模拟数据，接入真实 API 后出现 503 或连接拒绝。

## 根因分析
### 1. 进程与端口冲突 (Port & Process Conflicts)
*   **现象**: 系统中存在多个旧的 Node 和 Python 进程占用 3000 和 5000 端口。
*   **原因**: PM2 进程管理器在后台自动重启了名为 `invest-frontend` 的旧版服务，导致新启动的服务无法绑定端口。

### 2. 环境代理干扰 (Proxy Interference)
*   **现象**: 在终端使用 `curl` 访问 `localhost` 报错 503。
*   **原因**: 环境变量 `http_proxy` 指向了外部代理（127.0.0.1:20172），导致本地回环请求被错误转发。

### 3. 构建产物不完整 (Build Artifacts Issues)
*   **现象**: 构建后的 `.output` 目录缺失关键入口文件。
*   **原因**: 服务器内存受限（可用仅约 570MB），导致复杂的 Nuxt 4 构建过程被系统 OOM Killer 杀掉或中断。

### 4. 框架版本不兼容 (Nuxt 4 / Content v3 API)
*   **现象**: 页面运行时报错 `queryContent is not defined`。
*   **原因**: 升级到 Nuxt 4 后，`@nuxt/content` 的 API 从 `queryContent` 变更为 `queryCollection`。

## 解决方案
### 1. 彻底清理环境
使用 `fuser -k` 强制杀死占用端口的所有进程，并执行 `pm2 delete all` 清除陈旧的进程管理任务。

### 2. 绕过代理验证
在执行本地验证命令（如 `curl`）时，强制添加 `NO_PROXY=localhost,127.0.0.1`，确保请求直达本地服务。

### 3. 代码级修复
将 `index.vue` 和 `tutorials/index.vue` 中的数据获取逻辑更新为 Nuxt 4 兼容的 `queryCollection('tutorials').all()`。

### 4. 生产级部署 (PM2)
*   执行完整的 `npm run build` 生成 `.output` 生产包。
*   使用 PM2 直接启动二进制入口：`pm2 start .output/server/index.mjs --name "frontend-prod"`。
*   确保后端 `main.py` 监听 `0.0.0.0`，确保 Nginx 转发路径畅通。

## 经验总结
*   **生产环境改动必先构建**: Nuxt 应用在生产环境下运行的是 `.output` 目录，仅修改 `app/` 源码而不 `build` 是无效的。
*   **进程管理优先级**: 当系统安装有 PM2 时，必须先检查其状态，否则手动启动的进程会被 PM2 的自动重启策略覆盖或冲突。
*   **监控与日志是关键**: 通过 `tail -f /var/log/nginx/error.log` 能快速定位 502 的根源是 Upstream 连接失败。
*   **强制刷新**: 在单页应用（SPA）开发中，浏览器缓存极其顽固，必须提醒用户执行 **Ctrl + F5**。

## InvestCool 系统深度优化复盘 (2026-03-13)

### 1. 生产环境架构转换
- **现象**: 后台抓取线程在 Gunicorn 下失效。
- **原因**: 线程启动代码包裹在 `if __name__ == "__main__"` 块中，该块在 Gunicorn (WSGI) 加载时不会被执行。
- **方案**: 将启动逻辑移入应用生命周期的顶层，并增加 `DataWorker` 锁防止多进程环境下重复抓取。

### 2. 金融数据同步链路优化
- **现象**: 雅虎财经 (yfinance) 频繁出现 `NoneType` 报错或超时。
- **原因**: 服务器环境变量中的代理配置干扰了 SSL 直连请求。
- **方案**: 在同步函数中显式强制清空 `HTTP_PROXY` 环境变量，实现 100% 直连。
- **性能**: 通过 `yf.download` 的批量模式，将原本 10 秒以上的串行同步缩短至 2 秒以内。

### 3. 前端鲁棒性与可维护性
- **现象**: 首页内联样式过多，代码行数爆炸。
- **方案**: 完成了 `index.vue` 的 CSS 模块化重构，代码整洁度大幅提升。
- **感知**: 引入了“离线快照”UI 逻辑，即便后端暂时不可用，用户依然能看到最后一次的市场数据，显著提升了用户体验。

### 4. 内存极限构建
- **策略**: 在 1.6GB 总内存、仅余约 500MB 可用的环境下，通过限制 `NODE_OPTIONS="--max-old-space-size=512"`，成功实现了不停机（Hot Build）前端生产打包。


## [2026-03-19] InvestCool v2.5 全面升级：AI 决策内核与 Spike Pro UI 重构

### 1. AI 投资推荐系统 (Intelligence Module)
- **后端架构**:
    - 新增 AIRecommendation 数据库模型，存储状态（看多/看空/中性/观察）、分析摘要及点位。
    - 集成 gemini-cli：每 2 小时自动结合纳指 100 实时点位与科技巨头新闻生成犀利点评。
    - **高并发优化**: 引入内存缓存机制 (ai_latest_cache)，实现最新策略的微秒级响应。
    - **鲁棒性设计**: 增加 150 秒超时支持及「系统保底逻辑」，若 AI 生成失败，自动基于 RSI/VIX 指标提供启发式建议。
- **前端交互**:
    - 新增 AIRecommendationCard 组件：具备状态呼吸灯颜色标识及手动刷新功能。
    - 新增「策略足迹」时间轴：支持分页追溯 AI 的历史判断。

### 2. 全站 UI 深度重构 (Spike Pro 设计系统)
- **全局视觉规范**: 统一升级为 16px 大圆角 (var(--radius-lg))、多级柔和阴影 (var(--shadow-lg)) 及 Plus Jakarta Sans 现代科技字体。
- **首页 (Dashboard) 极限进化**:
    - 参考金融专业布局，重构为 12 列网格系统。
    - **MAG7 核心观察**: 将核心科技股看板移至黄金位置，独占全宽。
    - **极简指标列**: 纳指、市场情绪与 AI 状态整合为紧凑的垂直统计卡片组。
- **导航与侧边栏**:
    - **Header**: 引入磨砂玻璃特效 (backdrop-filter) 与导航底线滑入动效。
    - **侧边栏**: 极致精简，仅保留「关注我」（公众号/小红书双 QR 布局）与「每日交易锦报」。
    - **移动端**: 修复并优化了汉堡菜单的弹出逻辑、图标显示及层级覆盖。

### 3. 内容管理与规范化
- **内容逻辑解耦**: 
    - /analysis：纯净投资分析区，分类标准化为「投资入门、深度分析、公司基本面、AI」。
    - /tutorials：独立的技术教程区，承载系统架构类文章。
- **过滤机制**: 全站应用 JS 层级的稳健过滤，确保 is_deleted 为 true 的文章彻底不显示。
- **规范沉淀**: 将四大标准类目记录至 change.md，作为后续开发内容的刚性约束。

### 4. 生产部署优化
- **PM2 集群化**: 创建 ecosystem.config.js，统一管理前端 Nitro 与后端 Gunicorn 进程。
- **并发性能**: 优化 Gunicorn 为 2-worker 模式，适配 SQLite WAL 模式，平衡高性能与稳定性。
