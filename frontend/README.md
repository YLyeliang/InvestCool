# InvestCool Frontend

Next.js 16 App Router 前端。

```bash
npm ci
npm run dev
```

常用检查：

```bash
npm run lint
npm run typecheck
npm run build
```

开发服务器默认运行在 `http://localhost:3000`，并将 `/api/*` 转发到
`http://127.0.0.1:5000`。
